{
  "name": "@counterpick/overwolf-live",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/main.js && node --check src/protocol.js",
    "test": "node --test test/*.test.js",
    "verify": "npm run check && npm run build && npm run test"
  },
  "engines": {
    "node": ">=22.22.0",
    "npm": ">=10"
  },
  "devDependencies": {
    "sharp": "^0.35.3"
  }
}
